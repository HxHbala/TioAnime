<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/hentai_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimaryHen"
        android:minHeight="?attr/actionBarSize"
        app:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar"
        app:title="@string/change_hentai"
        app:titleTextColor="@color/colorText" />

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation_hen"
        android:layout_below="@id/hentai_toolbar">

        <WebView
            android:id="@+id/webView_hen"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/bottom_navigation"
            android:layout_below="@+id/toolbar"
            android:layout_centerHorizontal="true"
            android:background="@color/colorPrimaryDarkHen" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_hen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimaryHen"
        android:visibility="visible"
        app:itemIconTint="@color/colorAccentHen"
        app:itemTextColor="@color/colorAccentHen"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/bottom_nav_menu_hen"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_hen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_above="@+id/bottom_navigation_hen"
        android:layout_margin="16dp"
        android:clickable="true"
        android:focusable="true"
        android:onClick="saveFav"
        android:visibility="gone"
        app:backgroundTint="@color/colorAccentHen"
        app:srcCompat="@drawable/ic_favorite_border_white_24dp"/>
</RelativeLayout>